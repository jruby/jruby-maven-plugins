#set( $symbol_dollar = '$' )
<?xml version="1.0" encoding="UTF-8"?>
<project
    xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>${groupId}</groupId>
  <artifactId>${artifactId}</artifactId>
  <packaging>war</packaging>
  <version>${version}</version>
  <name>${artifactId} - rails${railsVersion.substring(0,1)} application</name>

  <repositories>
#literal()
    <!--
	the source for the gem artifacts
    -->
#end
    <repository>
      <id>rubygems-releases</id>
      <url>http://gems.saumya.de/releases</url>
    </repository>
#if( $railsVersion.matches(".*[a-zA-Z].*") )
    <repository>
      <id>rubygems-prereleases</id>
      <url>http://gems.saumya.de/prereleases</url>
    </repository>
#end
#literal()
    <!--
	remove this once there is no more SNAPSHOT of org.jruby.rack:jruby-rack
    -->
    <repository>
      <id>saumya</id>
      <url>http://mojo.saumya.de/</url>
      <releases>
	<enabled>false</enabled>
      </releases>
      <snapshots>
	<enabled>true</enabled>
	<updatePolicy>never</updatePolicy>
      </snapshots>
    </repository>
#end

  </repositories>

  <dependencies>
#literal()
    <!--
	jar dependencies
    -->
#end
    <dependency>
      <groupId>org.jruby.rack</groupId>
      <artifactId>jruby-rack</artifactId>
      <version>1.0.4.dev-SNAPSHOT</version>
    </dependency>
    <dependency>
      <groupId>org.jruby</groupId>
      <artifactId>jruby-complete</artifactId>
      <version>1.5.3</version>
    </dependency>
#literal()
    <!--
	gem dependencies
    -->
#end
    <dependency>
      <groupId>rubygems</groupId>
      <artifactId>activerecord-jdbc-adapter</artifactId>
      <version>0.9.4</version>
      <type>gem</type>
    </dependency>
    <dependency>
      <groupId>rubygems</groupId>
      <artifactId>rails</artifactId>
      <version>${railsVersion}</version>
      <type>gem</type>
    </dependency>
  </dependencies>
  <dependencyManagement>
    <dependencies>
      <dependency>
	<groupId>rubygems</groupId>
	<artifactId>jdbc-${database}</artifactId>
#literal()
        <!--
          for the stability of the build it is better to change this to
          specific working version
        -->
#end
	<version>[0.0.0,)</version>
	<type>gem</type>
      </dependency>
    </dependencies>
  </dependencyManagement>
  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
#literal()
    <!--
	this is needed for the maven-war-plugin configuration as well by
	the jetty plugin for the target/jetty/override-xyz-web.xml files
    -->
#end
    <gem.home>${symbol_dollar}{project.build.directory}/rubygems</gem.home>
    <gem.path>${symbol_dollar}{project.build.directory}/rubygems</gem.path>
    <jruby.plugins.version>0.24.0-SNAPSHOT</jruby.plugins.version>
    <jetty.version>7.1.0.RC1</jetty.version>
    <rails.env>development</rails.env>
  </properties>
  <build>
    <plugins>
#literal()
      <!--
	  this plugin gives you basic rubygems related goals and manages
	  the gems for maven. overview:
	  $ mvn gem:help
      -->
#end
      <plugin>
        <groupId>de.saumya.mojo</groupId>
        <artifactId>gem-maven-plugin</artifactId>
        <version>${symbol_dollar}{jruby.plugins.version}</version>
        <extensions>true</extensions>
	<executions>
#if( $railsVersion.substring(0,1) == '3' )
	  <execution>
	    <id>gemfile</id>
	    <phase>prepare-package</phase>
	    <goals><goal>exec</goal></goals>
	    <configuration>
	      <script>
		require 'fileutils'
		web_inf = File.join('${project.build.directory}', '${project.build.finalName}', 'WEB-INF')
		FileUtils.mkdir_p(web_inf)
		FileUtils.cp('${project.basedir}/Gemfile.maven', File.join(web_inf, 'Gemfile'))
	      </script>
	    </configuration>
	  </execution>
#end
	</executions>
      </plugin>
#literal()
      <!--
	  this plugin has some rails specific goals.
      -->
#end
      <plugin>
        <groupId>de.saumya.mojo</groupId>
        <artifactId>rails${railsVersion.substring(0,1)}-maven-plugin</artifactId>
        <version>${symbol_dollar}{jruby.plugins.version}</version>
#if( $railsVersion.substring(0,1) == '3' )
	<executions>
	  <execution>
	    <id>init</id>
	    <goals><goal>initialize</goal></goals>
	  </execution>
	</executions>
#end
      </plugin>
      <plugin>
        <artifactId>maven-war-plugin</artifactId>
        <version>2.1</version>
        <configuration>
          <webResources>
            <resource>
              <directory>public</directory>
            </resource>
            <resource>
              <directory>.</directory>
              <targetPath>WEB-INF</targetPath>
              <includes>
                <include>app/**</include>
                <include>config/**</include>
                <include>lib/**</include>
                <include>vendor/**</include>
#if( $database == "sqlite3")
#literal()
  <!--
      this might be not a good idea for production but for
      testing this should be OK. anyways it will not work when
      running the application from a packed war file
  -->
#end
                <include>db/*sqlite3</include>
#end
              </includes>
            </resource>
#literal()
     <!--
	 if you freeze your rails and unpack your gems you need to
	 comment/remove the next section to avoid ending up with
	 two sets of gems inside the war file
     -->
#end
            <resource>
              <directory>${symbol_dollar}{gem.path}</directory>
              <targetPath>WEB-INF/gems</targetPath>
            </resource>
          </webResources>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.mortbay.jetty</groupId>
        <artifactId>jetty-maven-plugin</artifactId>
        <version>7.0.1.v20091125</version>
	<configuration>
	  <webAppConfig>
	    <overrideDescriptor>${symbol_dollar}{project.build.directory}/jetty/override-${symbol_dollar}{rails.env}-web.xml</overrideDescriptor>
	  </webAppConfig>
	  <systemProperties>
	    <systemProperty>
	      <name>bundle.gemfile</name>
	      <value>Gemfile.maven</value>
	    </systemProperty>
	  </systemProperties>
	</configuration>
      </plugin>
    </plugins>
  </build>
  <profiles>
    <profile>
      <id>development</id>
      <activation>
	<activeByDefault>true</activeByDefault>
	<property>
          <name>rails.env</name>
          <value>development</value>
        </property>
      </activation>
      <dependencies>
	<dependency>
	  <groupId>rubygems</groupId>
	  <artifactId>jdbc-${database}</artifactId>
	  <version>[0.0.0,)</version>
	  <type>gem</type>
	</dependency>
#if( !$skipRspec == true )
	<dependency>
	  <groupId>rubygems</groupId>
	  <artifactId>rspec</artifactId>
	  <version>[1.3.0,1.4.0]</version>
	  <type>gem</type>
	</dependency>
	<dependency>
	  <groupId>rubygems</groupId>
	  <artifactId>rspec-rails</artifactId>
	  <version>1.3.2</version>
	  <type>gem</type>
	</dependency>
#end
      </dependencies>
    </profile>
    <profile>
      <id>test</id>
      <activation>
	<property>
          <name>rails.env</name>
          <value>test</value>
        </property>
      </activation>
      <dependencies>
#if( !$skipRspec == true )
	<dependency>
	  <groupId>rubygems</groupId>
	  <artifactId>rspec</artifactId>
	  <version>[1.3.0,1.4.0]</version>
	  <type>gem</type>
	</dependency>
	<dependency>
	  <groupId>rubygems</groupId>
	  <artifactId>rspec-rails</artifactId>
	  <version>1.3.2</version>
	  <type>gem</type>
	</dependency>
#end
	<dependency>
	  <groupId>rubygems</groupId>
	  <artifactId>jdbc-${database}</artifactId>
	  <version>[0.0.0,)</version>
	  <type>gem</type>
	</dependency>
      </dependencies>
      <build>
	<defaultGoal>test</defaultGoal>
	<plugins>
#if( !$skipRspec == true )
#literal()
          <!--
	      this plugin has some rspec specific goals.
          -->
#end
          <plugin>
	    <groupId>de.saumya.mojo</groupId>
	    <artifactId>rspec-maven-plugin</artifactId>
	    <version>${symbol_dollar}{jruby.plugins.version}</version>
            <executions>
	      <execution>
	        <phase>test</phase>
	        <goals><goal>test</goal></goals>
	      </execution>
	    </executions>
          </plugin>
#end
	  <plugin>
	    <groupId>de.saumya.mojo</groupId>
	    <artifactId>rails${railsVersion.substring(0,1)}-maven-plugin</artifactId>
	    <executions>
	      <execution>
		<id>test</id>
		<phase>test</phase>
		<goals><goal>rake</goal></goals>
		<configuration>
		  <task>test</task>
		</configuration>
	      </execution>
	    </executions>
	  </plugin>
	</plugins>
      </build>
    </profile>
    <profile>
      <id>production</id>
      <activation>
	<property>
          <name>rails.env</name>
          <value>production</value>
        </property>
      </activation>
      <dependencies>
	<dependency>
	  <groupId>rubygems</groupId>
	  <artifactId>jdbc-${database}</artifactId>
	  <version>[0.0.0,)</version>
	  <type>gem</type>
	</dependency>
      </dependencies>
      <properties>
        <gem.home>${project.build.directory}/rubygems-production</gem.home>
        <gem.path>${project.build.directory}/rubygems-production</gem.path>
      </properties>
    </profile>
    <profile>
      <id>war</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.mortbay.jetty</groupId>
            <artifactId>jetty-maven-plugin</artifactId>
            <version>${jetty.version}</version>
          </plugin>
        </plugins>
      </build>
    </profile>
    <profile>
      <id>run</id>
      <activation>
        <activeByDefault>true</activeByDefault>
      </activation>
      <build>
        <plugins>
          <plugin>
            <groupId>org.mortbay.jetty</groupId>
            <artifactId>jetty-maven-plugin</artifactId>
            <version>${jetty.version}</version>
            <configuration>
              <webAppConfig>
                <overrideDescriptor>${symbol_dollar}{project.build.directory}/jetty/override-${rails.env}-web.xml</overrideDescriptor>
              </webAppConfig>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
